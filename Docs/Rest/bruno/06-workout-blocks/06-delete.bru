meta {
  name: Delete Workout Block
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/api/workoutblocks/{{workoutBlockId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
