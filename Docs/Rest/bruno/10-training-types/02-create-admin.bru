meta {
  name: Create Training Type (Admin)
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/trainingtypes
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": "strength"
  }
}
