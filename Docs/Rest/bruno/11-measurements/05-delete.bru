meta {
  name: Delete Measurement
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/measurements/{{measurementId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
